x-logging:
  &default-logging
  logging:
    driver: json-file
    options:
      max-size: 100M

# cgroup v2 fix: cap_add/security_opt/cgroupns/volumes replace `privileged: true`
# for services that run isolate sandboxes. This gives the container:
#   - SYS_ADMIN: required for mount namespaces and cgroup operations in isolate
#   - seccomp:unconfined: isolate uses clone()/unshare() syscalls that a default
#     seccomp profile blocks
#   - privileged: true: shares the host cgroup namespace so /sys/fs/cgroup paths
#     seen inside the container match the real hierarchy
#   - /sys/fs/cgroup:rw: allows isolate to create sub-cgroups at runtime
# These options work on both cgroup v1 and v2 hosts.
### (x-isolate-runtime removed; unused)

services:
  server:
    image: bitblazer/judge0-cgv2:latest
    user: root
    volumes:
      - ./judge0.conf:/judge0.conf:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    ports:
      - "2358:2358"
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
    privileged: true
    pids_limit: -1
    <<: *default-logging
    restart: always

  worker:
    image: bitblazer/judge0-cgv2:latest
    user: root
    command: ["./scripts/workers"]
    volumes:
      - ./judge0.conf:/judge0.conf:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
    privileged: true
    pids_limit: -1
    <<: *default-logging
    restart: always

  db:
    image: postgres:16.2
    env_file: judge0.conf
    volumes:
      - data:/var/lib/postgresql/data/
    <<: *default-logging
    restart: always

  redis:
    image: redis:7.2.4
    command: [
      "bash", "-c",
      'docker-entrypoint.sh --appendonly no --requirepass "$$REDIS_PASSWORD"'
    ]
    env_file: judge0.conf
    <<: *default-logging
    restart: always

volumes:
  data:
